machine:
  php:
    version: 7.0.17

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install git-ftp

test:
  override:
    - echo "Testing hasn't been implemented yet"

deployment:
  production:
    branch: develop
    commands:
      - git ftp init -u $FTP_USERNAME -p $FTP_PASSWORD ftp://plesk01.swebby.se/oddstats.se/
